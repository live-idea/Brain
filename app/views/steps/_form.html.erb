<%= form_for [@quest,@step] do |f| %>
  <% if @step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@step.errors.count, "error") %> prohibited this step from being saved:</h2>

      <ul>
      <% @step.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="field">
    <%= f.label :photo %><br />
    <%= f.file_field :photo %>
  </div>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, :size => "40x5"%>
  </div>
  <div class="field">
    <%= f.label :result %><br />
    <%= f.text_field :result, :size => "35x5" %>
  </div>
  <div class="field">
    <%= f.label :video %><br />
    <%= f.text_area :video, :size => "40x5" %>
  </div>
  
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_area :title, :size => "40x5" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
