<%= form_for(@interesting) do |f| %>
  <% if @interesting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interesting.errors.count, "error") %> prohibited this interesting from being saved:</h2>

      <ul>
      <% @interesting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
